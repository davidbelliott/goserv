version: '3'

services:
  webapp:
    image: "goserv-webapp"
    networks:
      - container-network

  webserver:
    image: "goserv-webserver"
    ports:
      - "80:80"
      - "443:443"
    networks:
      - container-network
    volumes:
      - cert-volume:/etc/letsencrypt
    depends_on:
      - webapp

networks:
  container-network:

volumes:
  cert-volume:
